# 금속 3D 프린팅 결함 검출 및 분류 프로젝트

## 📋 프로젝트 개요

금속 3D 프린팅 공정 중 발생하는 결함을 **AprilGAN + CNN** 구조를 활용하여 검출하고 분류하는 프로젝트입니다.
**전체 시스템은 연합학습(Federated Learning) 프레임워크 내에서 동작**하며, 기업 간 데이터 공유 없이 모델 성능을 향상시킵니다.

## 🚀 시작하기

### 필수 요구사항

- **Python**: 3.8 이상 (권장: 3.9 이상)
- **pip**: Python 패키지 관리자
- **NVIDIA GPU**: CUDA를 지원하는 NVIDIA GPU
- **CUDA Toolkit**: GPU 가속을 위한 CUDA Toolkit (필수: CUDA 12.8)

### 설치 방법

자세한 설치 가이드는 [INSTALLATION.md](INSTALLATION.md)를 참고하세요.

1. **CUDA Toolkit 12.8 설치** (필수)
   - [NVIDIA CUDA Toolkit 다운로드](https://developer.nvidia.com/cuda-downloads)
   - 필수 버전: CUDA 12.8

2. **가상 환경 생성 및 활성화**
   ```powershell
   python -m venv venv
   .\venv\Scripts\Activate.ps1
   ```

3. **패키지 설치**
   ```powershell
   pip install -r requirements.txt
   ```

4. **PyTorch CUDA 12.8 버전 설치**
   ```powershell
   # CUDA 12.8 직접 지원 버전
   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
   ```

5. **환경 변수 설정**
   - 프로젝트 루트에 `.env` 파일 생성
   - MongoDB 연결 정보 입력 (자세한 내용은 [INSTALLATION.md](INSTALLATION.md) 참고)

자세한 설치 가이드는 [INSTALLATION.md](INSTALLATION.md)를 참고하세요.

## 🎯 목표

- **결함 검출**: AprilGAN을 통한 합성 데이터 생성 및 결함 탐지
- **결함 분류**: CNN을 통한 결함 클래스 분류
- 제한된 데이터 환경에서의 성능 개선 및 실용성 확보
- **연합학습**: 기업 간 데이터 공유 없이 협력 학습

## 🏗️ 모델 구조

### 연합학습 기반 AprilGAN + CNN 파이프라인

```
[클라이언트 1]                    [클라이언트 2]                    [클라이언트 N]
    ↓                                 ↓                                 ↓
로컬 데이터 학습              로컬 데이터 학습              로컬 데이터 학습
    ↓                                 ↓                                 ↓
가중치 전송 ──────────────────────────────────────────→ [서버]
                                                          ↓
                                                    가중치 평균 계산
                                                          ↓
가중치 수신 ←───────────────────────────────────────────
    ↓
평균화된 가중치로 모델 업데이트
```

### AprilGAN + CNN 파이프라인

```
원본 이미지
    ↓
[AprilGAN] 제로샷 이상 탐지 (학습 불필요)
    ↓
이상 영역 마스크/좌표 (어디에 이상이 있는지)
    ↓
[CNN] 결함 유형 분류 (연합학습)
    ↓
결함 유형 ("Super Elevation", "Crack", etc.)
```

- **AprilGAN**: 제로샷 비전 이상탐지 모델 - 추가 학습 없이 바로 사용 가능
- **CNN**: 결함 유형별 분류 성능 극대화 - 연합학습으로 협력 학습

### 핵심 원칙 (Critical Requirements)

**⚠️ 데이터 프라이버시 보장**
- ✅ **데이터는 절대 공유하지 않음**: 각 클라이언트(기업)의 금속 공정 결함 데이터는 로컬에서만 사용
- ✅ **가중치만 전송**: 학습된 모델의 가중치(weights)만 서버로 전송
- ✅ **서버에서 평균화**: 서버는 수신한 가중치들의 평균값을 계산 (Federated Averaging)
- ✅ **평균 가중치 배포**: 계산된 평균 가중치를 모든 클라이언트에 배포

### 구조 변경 이유

1. **데이터 제약**: 제한된 실제 데이터셋으로 인한 U-Net 기반 분할 모델의 한계
2. **효율성**: 분류 중심 과제에 적합한 구조로 재설계
3. **확장성**: GAN 기반 데이터 증강을 통한 모델 일반화 능력 향상
4. **프라이버시**: 연합학습을 통한 기업 간 데이터 공유 없이 협력 학습

## 🗂️ 프로젝트 구조

프로젝트는 다음과 같이 구성되며, **전체가 연합학습 프레임워크 내에서 동작**합니다:

```
GAN-CNN/
├── models/              # 모델 구현
│   ├── aprilgan.py      # AprilGAN 제로샷 모델 래퍼
│   └── cnn.py           # CNN 결함 분류 모델
├── federated/           # 연합학습 프레임워크
│   ├── server.py        # 연합학습 서버 (가중치 집계)
│   ├── client.py        # 연합학습 클라이언트 (로컬 학습)
│   └── aggregator.py    # Federated Averaging 알고리즘
├── utils/               # 유틸리티
│   ├── data_loader.py   # 데이터 로딩 및 전처리
│   └── bbox_utils.py    # 바운딩박스 처리
├── demo/                # 데모 및 예제
│   └── federated_learning_demo.ipynb  # 전체 파이프라인 시연
├── data/                # 데이터 디렉토리
└── utils/dataset/       # 데이터셋 관리 스크립트
```

### 주요 모듈 설명

1. **데이터 처리 (Data Processing)**: 데이터 가져오기 및 전처리
   - 각 클라이언트는 자체 데이터만 처리 (데이터는 외부로 전송되지 않음)

2. **AprilGAN (Defect Detection)**: 제로샷 이상 탐지
   - 사전 학습된 모델로 추가 학습 불필요
   - 모든 클라이언트에서 동일한 모델 사용
   - 이상 영역을 자동으로 검출

3. **CNN (Defect Classification)**: 결함 유형별 분류
   - AprilGAN이 찾은 이상 영역을 입력으로 받아 분류
   - 로컬 데이터로 학습 → 가중치만 서버로 전송

4. **연합 학습 (Federated Learning)**: 서버-클라이언트 구조 구현
   - **서버 (Server)**:
     - 클라이언트로부터 가중치 수신
     - Federated Averaging 알고리즘으로 가중치 평균 계산
     - 평균화된 가중치를 클라이언트에 배포
   - **클라이언트 (Client)**:
     - 로컬 데이터로 모델 학습 (데이터는 절대 외부 전송 안 함)
     - 학습된 가중치를 서버로 전송
     - 서버로부터 평균화된 가중치 수신 및 모델 업데이트

## 🔍 AprilGAN의 결함 검출 및 데이터 생성

### AprilGAN의 역할 (제로샷 비전 이상탐지 모델)

**AprilGAN은 사전 학습된 제로샷 모델입니다:**
- ✅ **학습 불필요**: 추가 학습 없이 바로 사용 가능
- ✅ **전처리 불필요**: 원본 이미지를 그대로 입력
- ✅ **자동 이상 탐지**: 이상 영역을 자동으로 찾음
- ✅ **자동 Localization**: 이상 위치를 자동으로 식별

**AprilGAN의 한계:**
- ❌ 이상 유무는 판단 가능하지만, **어떤 종류의 결함인지는 분류 불가**

### CNN의 역할 (결함 유형 분류)

**CNN은 AprilGAN이 찾은 이상 영역을 분류합니다:**
- AprilGAN이 검출한 이상 영역을 입력으로 받음
- 각 이상 영역이 어떤 결함 유형인지 분류
- 기존 CNN과 달리 전처리나 localization 불필요 (AprilGAN이 이미 처리)

### 데이터 전처리 과정

1. **바운딩박스 → 마스크 변환**
   - JSON의 StartPoint/EndPoint를 이진 마스크로 변환
   - CNN 학습용 레이블로 사용

2. **결함 유형 추출**
   - TagBoxes의 Comment 필드에서 결함 유형 추출
   - CNN 분류 모델의 클래스 레이블로 사용

3. **AprilGAN 이상 탐지**
   - 제로샷 모델로 이상 영역 자동 검출
   - 검출된 영역과 실제 레이블 매칭하여 CNN 학습 데이터 생성

## 🔄 연합학습에서의 두 모델 처리 전략

### 통합 연합학습 방식 (권장)

AprilGAN과 CNN은 순차적 파이프라인 구조이므로, **CNN만 연합학습**을 수행합니다.

**클라이언트 측 프로세스:**
1. 서버로부터 최신 CNN 가중치 수신
2. 로컬 이미지에 AprilGAN 적용 (제로샷, 학습 없음)
3. AprilGAN이 찾은 이상 영역 추출
4. 이상 영역을 CNN으로 분류하도록 학습
5. CNN 가중치만 서버로 전송

**서버 측 프로세스:**
1. 모든 클라이언트로부터 CNN 가중치 수신
2. CNN 가중치 평균화 (Federated Averaging)
3. 평균화된 CNN 가중치를 클라이언트에 배포

**장점:**
- 통신 효율성: CNN 가중치만 전송
- 모델 동기화: 모든 클라이언트가 동일한 AprilGAN 사용
- 구현 단순성: 하나의 연합학습 라운드로 관리

## 📊 데이터셋

- 금속 3D 프린팅 공정 결함 이미지
- JSON 파일에 바운딩박스 형태로 결함 정보 저장
- 결함 유형별 샘플 불균형 문제 해결을 위한 AprilGAN 기반 증강

## 📈 기대 효과

- **데이터 확장**: GAN 기반 합성 데이터로 훈련 세트 대폭 확장
- **성능 개선**: CNN 분류 모델의 정확도 향상
- **불균형 해소**: 결함 유형별 데이터 불균형 문제 완화
- **프라이버시 보장**: 기업 간 데이터 공유 없이 협력 학습

## 🔒 연합학습 보안 및 프라이버시

### 데이터 보호
- **로컬 학습**: 모든 학습은 클라이언트의 로컬 환경에서 수행
- **가중치만 전송**: 원본 데이터나 전처리된 데이터는 절대 전송하지 않음
- **기업 간 격리**: 각 기업의 데이터는 다른 기업과 공유되지 않음

### 연합학습 라운드
1. **초기화**: 서버가 초기 모델 가중치를 클라이언트에 배포
2. **로컬 학습**: 각 클라이언트가 자체 데이터로 모델 학습
3. **가중치 업로드**: 학습된 가중치를 서버로 전송
4. **집계**: 서버가 모든 클라이언트의 가중치를 평균화
5. **배포**: 평균화된 가중치를 클라이언트에 배포
6. **반복**: 2-5단계를 반복하여 모델 성능 향상

## 🎮 사용 방법

### 1. 데이터 준비

```python
from utils.dataset.download_labeled_layers import main as download_data
download_data()  # MongoDB에서 데이터 다운로드
```

### 2. 데이터 분석

```python
from utils.dataset.analyze_defect_types import analyze_defect_dataset
analyze_defect_dataset(data_dir="data")
```

### 3. 연합학습 실행

Jupyter notebook 데모를 실행하세요:

```bash
jupyter notebook demo/federated_learning_demo.ipynb
```

또는 Python 스크립트로 실행:

```python
from federated.server import FederatedServer
from federated.client import FederatedClient

# 서버 시작
server = FederatedServer(port=5000)
server.start()

# 클라이언트 생성 및 학습
client = FederatedClient(client_id=0, server_url='http://localhost:5000')
# ... 학습 코드 ...
```

## 📝 참고문헌

- 논문 2: GAN 기반 합성 데이터 생성 방법론
- 베이스라인: U-Net 기반 연합 학습 결함 분할

---

## 📚 문서

- [설치 가이드](INSTALLATION.md) - 환경 설정 및 패키지 설치 방법
- [요구사항](requirements.txt) - 필수 패키지 목록

---

**Last Updated**: 2025년
