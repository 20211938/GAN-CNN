# Python 3.8 이상 필요 (권장: 3.9 이상)
# 자세한 버전 요구사항은 INSTALLATION.md 참고
#
# 설치 순서:
# 1. 가상 환경 활성화 필수!
# 2. 기본 패키지 설치: pip install -r requirements.txt
# 3. PyTorch CUDA 12.8 설치: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# ⚠️ 주의: PyTorch는 별도로 CUDA 버전을 지정하여 설치해야 합니다!

# ============================================================================
# 필수 패키지 (기본 설치)
# ============================================================================

# 수치 연산 및 데이터 처리
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0

# 이미지 처리 (OpenCV - cv2 모듈)
opencv-python>=4.8.0,<5.0.0
Pillow>=10.0.0,<11.0.0

# 데이터 시각화
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# 머신러닝 유틸리티
scikit-learn>=1.3.0,<2.0.0
scikit-image>=0.21.0,<1.0.0

# 진행 상황 표시
tqdm>=4.65.0,<5.0.0

# 네트워크 통신 (연합학습용)
flask>=2.3.0
requests>=2.31.0

# 데이터베이스 연결
pymongo>=4.6.0,<5.0.0

# 환경 변수 관리
python-dotenv>=1.0.0,<2.0.0

# Jupyter notebook
jupyter>=1.0.0
ipykernel>=6.25.0

# ============================================================================
# 딥러닝 프레임워크 (별도 설치 필요!)
# ============================================================================
# PyTorch는 CUDA 버전에 맞춰 별도로 설치해야 합니다:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# CPU 버전만 필요한 경우:
# pip install torch torchvision
#
# torch>=2.0.0
# torchvision>=0.15.0
